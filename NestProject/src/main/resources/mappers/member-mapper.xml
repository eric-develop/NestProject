<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Member_mapper">
		<insert id="insertMember" parameterType="Member">
			INSERT INTO MEMBER VALUES(SEQ_MNO.NEXTVAL,DEFAULT,#{userId},#{password},#{userName},#{nickName},#{phone},#{ageGroup},#{purpose},#{jobField},DEFAULT,DEFAULT,DEFAULT,DEFAULT)
		</insert>
		
		<select id="idDupCheck" parameterType="string" resultType="_int">
			SELECT COUNT(*) FROM MEMBER WHERE USERID= #{userId}
		</select>
		
		<select id="nickDupCheck" parameterType="string" resultType="_int">
			SELECT COUNT(*) FROM MEMBER WHERE NICKNAME = #{nickName}
		</select>
		
</mapper>